[package]
name = "crates_io_og_image"
version = "0.3.1"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "OpenGraph image generation for crates.io"
repository = "https://github.com/rust-lang/crates_io_og_image"

[lints.clippy]
dbg_macro = "warn"
todo = "warn"

[lints.rust]
future_incompatible = "warn"
nonstandard_style = "warn"
rust_2018_idioms = "warn"
rust_2018_compatibility = "warn"
rust_2021_compatibility = "warn"
unused = "warn"

[lints.rustdoc]
unescaped_backticks = "warn"

[features]
default = ["oxipng"]
oxipng = ["dep:oxipng"]

[dependencies]
oxipng = { version = "10.1.1", optional = true, default-features = false, features = ["parallel", "zopfli"] }
reqwest = "0.13.4"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.150"
thiserror = "2.0.18"
tokio = { version = "1.52.3", features = ["rt"] }
tracing = "0.1.44"
typst = "0.15.0"
typst-kit = { version = "0.15.0", default-features = false, features = ["scan-fonts", "embedded-fonts"] }
typst-layout = "0.15.0"
typst-render = "0.15.0"

[dev-dependencies]
insta = "=1.48.0"
mockito = "=1.7.2"
tokio = { version = "=1.52.3", features = ["macros", "rt-multi-thread"] }
tracing-subscriber = { version = "=0.3.23", features = ["env-filter", "fmt"] }

[package.metadata.release]
sign-commit = true
sign-tag = true
pre-release-commit-message = "Release v{{version}}"
tag-message = ""
publish = false
